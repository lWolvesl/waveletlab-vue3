# ICCWAMTIP 2026 网站维护指南

## 目录

- [1. 项目概况](#1-项目概况)
- [2. 目录与架构](#2-目录与架构)
  - [页面层](#页面层)
  - [公共组件](#公共组件)
  - [路由](#路由)
- [3. 中英文维护](#3-中英文维护)
  - [修改会议日期](#修改会议日期)
  - [共享数据](#共享数据)
- [4. 文件上传位置](#4-文件上传位置)
  - [网站仓库](#网站仓库)
  - [静态资源仓库](#静态资源仓库)
- [5. jsDelivr 链接](#5-jsdelivr-链接)
  - [超过 20 MB 的文件](#超过-20-mb-的文件)
- [6. AI 辅助维护流程](#6-ai-辅助维护流程)
- [7. GitHub 协作流程](#7-github-协作流程)
  - [第一次参与维护](#第一次参与维护)
  - [开始一次修改](#开始一次修改)
  - [修改与验证](#修改与验证)
  - [创建 Pull Request](#创建-pull-request)
  - [审核与合并](#审核与合并)
  - [合并后的清理](#合并后的清理)
  - [AI 参与协作时](#ai-参与协作时)
- [8. GitHub Pages 部署](#8-github-pages-部署)
- [9. 发布前检查清单](#9-发布前检查清单)

## 1. 项目概况

本项目是一个使用 Vue 3 和 Vite 构建的中英文会议网站。

主要技术：

- Vue 3：页面和组件
- Vue Router：页面路由
- Element Plus：按钮等界面组件
- GitHub Actions：自动构建并部署到 GitHub Pages
- jsDelivr：托管公开静态资源
- GitHub Raw：托管超过 jsDelivr 单文件限制的大文件

常用命令：

```bash
npm install
npm run dev
npm run build
```

提交代码前必须运行 `npm run build`，确认没有构建错误。

## 2. 目录与架构

```text
.
├── .github/workflows/deploy.yml  # GitHub Pages 自动部署
├── public/                       # 随网站一起部署的少量静态文件
├── scripts/copy-routes.mjs       # 生成兼容 .html 地址的入口文件
├── src/
│   ├── assets/styles.css         # 全站样式
│   ├── components/               # 公共组件
│   ├── i18n/index.js             # 中英文内容及主要会议数据
│   ├── pages/                    # 各页面
│   ├── router/index.js           # 路由配置
│   ├── App.vue                   # 应用入口组件
│   └── main.js                   # Vue 初始化
├── index.html
├── package.json
└── vite.config.js
```

### 页面层

`src/pages/` 中每个文件对应一个页面：

| 文件 | 页面 |
| --- | --- |
| `HomePage.vue` | 首页 |
| `InformationPage.vue` | 会议信息 |
| `GuidancePage.vue` | 作者指南 |
| `TimelinePage.vue` | 时间线 |
| `TopicsPage.vue` | 征稿主题 |
| `SpeakersPage.vue` | 大会报告 |
| `CommitteesPage.vue` | 组织委员会 |
| `ContactPage.vue` | 联系方式 |
| `NewsPage.vue` | 新闻列表 |

新增页面时，需要同时：

1. 在 `src/pages/` 创建页面组件；
2. 在 `src/router/index.js` 注册路由；
3. 在中英文导航中添加入口；
4. 必要时在 `scripts/copy-routes.mjs` 中加入对应 `.html` 地址。

### 公共组件

`src/components/` 保存页头、页脚、侧栏、数据表等共享结构。多个页面都会使用的界面应优先做成公共组件，不要在各页面重复复制。

### 路由

路由定义在 `src/router/index.js`。网站使用 History 路由，同时保留了原站的 `.html` 地址，例如：

```text
/information.html
/guidance.html
/timeline.html
```

页面标题通过路由中的 `meta.titleKey` 从中英文数据中读取。

## 3. 中英文维护

本项目没有使用第三方翻译平台，中英文内容集中保存在：

```text
src/i18n/index.js
```

主要规则：

- `en` 是英文内容；
- `zh` 是中文内容；
- 两种语言的数据结构应保持一致；
- 同一字段在中英文中必须使用相同的键名；
- 修改日期、链接、人员、新闻时要同时检查中英文；
- 语言选择保存在浏览器的 `iccwamtip-locale` 中。

例如新增新闻时，应同时修改两个 `latestNews`：

```js
// 英文
latestNews: [
  {
    date: '2026-07-31',
    text: 'The paper submission deadline has been extended to August 31st, 2026.',
  },
]

// 中文
latestNews: [
  {
    date: '2026-07-31',
    text: '论文投稿截止日期已延期至2026年8月31日。',
  },
]
```

新闻按时间倒序排列，最新内容放在数组最前面。

### 修改会议日期

会议日期通常不只出现一次。修改日期后至少检查：

- `importantDates`
- `latestNews`
- `timelineRows`
- 首页介绍文字
- 作者指南
- 下载版征稿通知

可使用以下命令检查旧日期是否残留：

```bash
rg "August 10|8月10" src
```

历史新闻应保留当时的日期。例如 7 月 31 日宣布从 8 月 10 日延期到 8 月 31 日时，7 月 7 日的旧新闻仍应保留 8 月 10 日，否则新闻时间线会自相矛盾。

### 共享数据

邮箱、电话、投稿地址等共享内容位于 `sharedConferenceLinks`。出版编号位于 `catalogNumbers`，嘉宾图片映射位于 `speakerImages`。共享信息应在这些公共变量中修改，避免中英文分别维护后产生差异。

## 4. 文件上传位置

### 网站仓库

仓库：

```text
https://github.com/lWolvesl/waveletlab-vue3
```

`public/` 只保留体积较小、与网站运行紧密相关的文件：

```text
public/
├── CNAME
├── doc/
│   └── Instructions for using the Microsoft CMT system.pdf
└── images/
    ├── logo.png
    ├── wavelet-logo-with-text.svg
    └── 嘉宾头像
```

不要把大型 PDF、历年录用通知或经常更新的下载文件重新放回网站仓库，否则会增大 Git 历史和部署产物。

### 静态资源仓库

公开下载文件上传到：

```text
https://github.com/waveletlab-uestc/oos
```

建议按年份整理：

```text
oos/
├── 2026/
│   ├── introduction2026.png
│   ├── QQGroup2026.jpg
│   └── iccwamtip2026_latex_templates-260824.zip
├── CallforPaper-CN260731.pdf
├── CallforPaper-EN260731.pdf
├── PaperFormat-2026(CN)-260731.dotx
├── PaperFormat-2026(EN)-260731.dotx
└── booklet2023.pdf
```

以后新增文件建议全部放进年份目录，例如：

```text
2026/docs/
2026/images/
2026/templates/
```

文件名应包含年份或更新日期，不要直接覆盖旧文件。例如：

```text
CallforPaper-EN260831.pdf
PaperFormat-2026(EN)-260831.dotx
```

## 5. jsDelivr 链接

小于 20 MB 的公开文件使用 jsDelivr：

```text
https://cdn.jsdelivr.net/gh/组织名/仓库名@提交SHA/文件路径
```

示例：

```text
https://cdn.jsdelivr.net/gh/waveletlab-uestc/oos@提交SHA/2026/introduction2026.png
```

获取最新提交 SHA：

```bash
git ls-remote https://github.com/waveletlab-uestc/oos.git refs/heads/main
```

正式网站应固定完整提交 SHA，不建议使用 `@main`。固定 SHA 可以避免缓存导致新旧文件混用。

上传资源后的流程：

1. 将文件提交并推送到 `waveletlab-uestc/oos`；
2. 获取 `main` 最新的完整提交 SHA；
3. 拼接 jsDelivr 地址；
4. 在浏览器中确认文件能正常打开；
5. 替换网站源码中的旧链接；
6. 搜索旧文件名，确认没有遗漏；
7. 执行 `npm run build`。

检查地址是否可访问：

```bash
curl -I "https://cdn.jsdelivr.net/gh/waveletlab-uestc/oos@提交SHA/文件路径"
```

### 超过 20 MB 的文件

jsDelivr 的 GitHub CDN 对超过 20 MB 的单个文件会返回 403。此类文件使用固定提交的 GitHub Raw 地址：

```text
https://raw.githubusercontent.com/组织名/仓库名/提交SHA/文件路径
```

当前 `booklet2023.pdf` 就使用 GitHub Raw。更大的长期下载文件也可以考虑对象存储，而不是继续增大 Git 仓库。

## 6. AI 辅助维护流程

推荐每次只给 AI 一个明确任务，并要求它先检查再修改。

### 第一步：描述需求

说明要修改的内容、日期、语言和文件来源。例如：

```text
把投稿截止日期延期到 2026 年 8 月 31 日。
同步修改中英文的重要日期、时间线和最新新闻。
历史新闻保留原日期。
修改后运行构建并检查是否还有旧日期。
```

上传新资源时可以使用：

```text
文件已经上传到 waveletlab-uestc/oos 的 2026 目录。
请获取最新提交 SHA，验证 jsDelivr 地址，然后替换项目中的旧链接。
超过 20 MB 的文件使用 GitHub Raw。
不要修改其他内容。
```

### 第二步：让 AI 检查影响范围

要求 AI 使用全文搜索确认同一信息是否出现在多个地方：

```text
先搜索所有相关日期、文件名和旧链接，列出影响位置后再修改。
```

### 第三步：修改和验证

每次修改完成后至少执行：

```bash
npm run build
git diff --check
git diff
```

对远程资源还应验证 HTTP 状态、文件大小和文件类型。

### 第四步：人工复核

人工重点检查：

- 中英文是否都已更新；
- 日期是否前后一致；
- 新闻时间顺序是否合理；
- 下载链接是否指向正确版本；
- 首页、移动端和二维码显示是否正常；
- AI 是否误改了无关文件；
- `git status` 中是否包含不应提交的文件。

### 第五步：提交

```bash
git add -A
git commit -m "更新投稿截止日期和静态资源"
git push
```

推送后在 GitHub Actions 中确认部署成功，再访问正式网站复核。

## 7. GitHub 协作流程

网站代码通过 **Fork + Pull Request** 协作。除仓库管理员处理紧急问题外，不要直接向主仓库的 `main` 分支推送。

主仓库：

```text
https://github.com/lWolvesl/waveletlab-vue3
```

### 第一次参与维护

1. 打开主仓库页面；
2. 点击右上角 **Fork**；
3. 将 Fork 创建到自己的 GitHub 账号；
4. 克隆自己的 Fork，而不是直接克隆主仓库。

```bash
git clone https://github.com/你的用户名/waveletlab-vue3.git
cd waveletlab-vue3
```

克隆后添加主仓库为 `upstream`：

```bash
git remote add upstream https://github.com/lWolvesl/waveletlab-vue3.git
git remote -v
```

正常情况下：

```text
origin    自己的 Fork
upstream  官方主仓库
```

### 开始一次修改

每次开始工作前，先同步主仓库：

```bash
git switch main
git fetch upstream
git merge --ff-only upstream/main
git push origin main
```

然后创建独立分支，不要直接在 `main` 上修改：

```bash
git switch -c update-submission-deadline
```

分支名应简短并说明任务，例如：

```text
update-submission-deadline
update-call-for-paper
fix-mobile-navigation
add-2026-speaker
```

### 修改与验证

完成修改后，先检查：

```bash
npm install
npm run build
git diff --check
git status
git diff
```

确认中英文、日期、远程资源和页面显示都正确后再提交：

```bash
git add -A
git commit -m "更新投稿截止日期"
git push -u origin update-submission-deadline
```

### 创建 Pull Request

推送分支后，在自己的 GitHub Fork 页面点击 **Compare & pull request**。

Pull Request 应设置为：

```text
base repository: lWolvesl/waveletlab-vue3
base branch: main
head repository: 你的用户名/waveletlab-vue3
compare branch: 本次修改分支
```

PR 标题应直接说明结果，例如：

```text
更新 2026 年投稿截止日期
```

PR 描述建议包含：

```markdown
## 修改内容

- 将投稿截止日期更新为 2026 年 8 月 31 日
- 同步更新中英文重要日期和时间线
- 新增延期新闻

## 验证

- [x] npm run build
- [x] 中英文内容已同步
- [x] 已检查旧日期残留
```

### 审核与合并

仓库管理员审核时重点检查：

- 修改是否只包含本次任务；
- 中英文是否同步；
- 日期和新闻是否前后一致；
- 是否包含不应提交的 `node_modules/`、`dist/` 或大型文件；
- 外部资源链接是否可以访问；
- GitHub Actions 构建是否通过。

需要修改时，在原分支继续修改并推送即可，PR 会自动更新：

```bash
git add -A
git commit -m "根据审核意见调整"
git push
```

审核通过后由仓库管理员合并 PR。一般使用 **Squash and merge**，让一次维护任务在 `main` 中对应一个清晰提交。

### 合并后的清理

PR 合并后，在 GitHub 页面删除远程功能分支，然后同步本地：

```bash
git switch main
git fetch upstream
git merge --ff-only upstream/main
git push origin main
git branch -d update-submission-deadline
```

下一次任务重新从最新的 `main` 创建新分支，不要继续复用已经合并的旧分支。

### AI 参与协作时

向 AI 提交任务时，应明确协作边界：

```text
当前修改将通过 Fork 和 Pull Request 提交。
请只修改本次任务相关文件，不要直接推送 main，不要强制推送。
完成后运行 npm run build 和 git diff --check，并给出 PR 标题和描述建议。
```

AI 可以协助修改、检查和生成 PR 描述，但提交前仍需要人工检查 `git diff`。除非明确授权，不应让 AI 自动合并 PR 或删除远程分支。

## 8. GitHub Pages 部署

部署工作流位于：

```text
.github/workflows/deploy.yml
```

当前仓库默认分支为 `main`，工作流中的触发分支也必须是 `main`：

```yaml
on:
  push:
    branches:
      - main
```

如果工作流仍写成 `master`，推送到 `main` 时不会自动部署。GitHub 仓库的 Pages 设置中，Source 应选择 **GitHub Actions**。

构建过程：

```text
推送 main
→ GitHub Actions 安装依赖
→ npm run build
→ 上传 dist
→ 部署 GitHub Pages
```

## 9. 发布前检查清单

- [ ] 中英文内容同时更新
- [ ] 当前日期在首页、时间线和信息页保持一致
- [ ] 历史新闻没有被错误改写
- [ ] 新资源已上传到 `waveletlab-uestc/oos`
- [ ] jsDelivr 或 GitHub Raw 地址返回 200
- [ ] 远程地址固定了完整提交 SHA
- [ ] 源码中没有残留旧文件名或旧日期
- [ ] 没有把大型资源放回网站仓库
- [ ] `npm run build` 成功
- [ ] `git diff --check` 通过
- [ ] 修改位于独立功能分支
- [ ] 已通过 Pull Request 审核
- [ ] GitHub Actions 部署成功
- [ ] 正式网站桌面端和移动端显示正常
